# Local/LAN acceptance — 2026-08-19

## Scope and result

This is the real local/provider continuation of T603. It covers the local
PostgreSQL, Redis, MinIO, Go API/media worker, Python `nh_media` worker, web
client, reviewed FFmpeg/ffprobe pair, and the LAN server path. It does not
claim public/VPS/TLS/load/power-loss or remote API quality.

- Branch: `implementation/bootstrap`
- Baseline before this task: `a97b77304af195f623b067d3202df83b752ede8c`
- Reviewed FFmpeg SHA-256: `af9e7af850346ae908745f6191cdcf9581889e915f8fe2dd6ab8cabef21161d9`
- Reviewed ffprobe SHA-256: `6d2b7ac8cd07da82f066994be8bcf0c74ab1aba7f248bb75025fcc543711181a`
- Local AI provider smoke: PASS for Ollama LLM/VLM/embedding, Windows SAPI
  TTS, and CPU/int8 faster-whisper ASR. See
  `real-local-provider-smoke-20260819-r3.json`.
- Local Product API E2E: PASS after the repair runs below.
- LAN server path: PASS at the private server address used by this host.
- Physical second device: NOT_PASS/EXTERNAL INPUT REQUIRED. The client script
  was intentionally run on the server and rejected same-machine execution;
  see `lan-client-20260819-not-pass.json`.

## Generated corpus

The corpus is owned/generated test media: no third-party footage, image, voice
or music. The full ffprobe manifest is the external manifest generated by
`tools/real-local-corpus.ps1`; the values below are the immutable inputs used
by the Product API traces.

| Input | SHA-256 | Shape | Duration | Notes |
|---|---|---:|---:|---|
| `corpus-landscape.mp4` | `39a047b207d97384363e43db87c6a30a6d0b56a20994a1065cffb9ab9ee80814` | 1280x720 (16:9) | 12 s | H.264/AAC, 24 fps |
| `corpus-portrait.mp4` | `84bd8d2826e3de70241c0a111c3254ba33f66b84e705ccbdde58f423c06a9fc8` | 720x1280 (9:16) | 12 s | H.264/AAC, 24 fps |
| `corpus-long-landscape-960x540.mp4` | `e3a77b627b68fc1d8039decad082caa394a7d9965598e2be7c9cf069dc28cdb8` | 960x540 (16:9) | 24 s | H.264/AAC, 30 fps |
| `narration.wav` | `3a2a1b12b777bb183b43d12898fe70d2ebb2263940f684979f4c9d49544baa4a` | WAV | generated | Windows SAPI corpus narration |
| `corpus.srt` | `8e5ee7eceddf83f81361df25444eb0c9fdb260f1b8b4e65bc0688cb27a12821f` | sidecar | 5 cues | subtitle input/validation sidecar |

The subtitle-bearing MP4 was used only for corpus validation because its
subtitle stream is intentionally outside the Product API's declared upload
stream policy; `corpus.srt` remains the sidecar evidence.

## Consecutive full Product API E2E runs

Each PASS below has a Project, upload/validation Job, 25-step movie Job,
PipelineRun, approved TimelineVersion, Render and Render Job, real provider
snapshot, clip/export artifacts, deliverable QA, and download SHA-256 checks.
The JSON files contain the complete artifact-ref and per-download trace.

| Run | Input / profile | Project | Movie Job | PipelineRun | TimelineVersion | Render / Render Job | Steps | Downloads |
|---|---|---|---|---|---|---|---:|---:|
| r14 | 24 s landscape / `square_1_1` | `7c83c411-a59b-4076-9e8b-fdb773affc0f` | `497d63ad-4232-4f90-ad4e-31a3df6e2f5c` | `52d3d545-e4b5-4f58-9a7d-640ff1dd7e47` | `e992b489-0ef8-4eb3-a9f4-9d43074a2663` | `2b6d2fe8-b9e6-4bec-a16d-a8b13903bf24` / `ecfbf5e4-47a5-4c68-8c0a-a5c4a3f7c924` | 25/25 | 0 mismatches |
| r15 | landscape / `shorts_9_16` | `1209c8e1-a3a3-496f-b61b-19724ff4ba13` | `645c131d-d889-42d9-92f8-7d8702570fcf` | `bd171ee2-1b1c-4267-8d35-308ca93cfd87` | `63685d90-81f3-45e0-9718-fb9bff90d37c` | `2b085230-f144-4fef-bb2d-035876bc2106` / `1cdc94d0-ba1b-4ab7-8624-89f51651c6d5` | 25/25 | 0 mismatches |
| r16 | portrait / `youtube_16_9` | `0e257c33-b031-42a8-84de-593bb4dcd4bc` | `3fabb550-979d-4228-9535-a0854366a6fc` | `27b89fc0-f91f-463a-bc8e-4d011df59c21` | `abcb1939-59dc-468d-a649-7b2ee0440843` | `2ae58bd3-4670-4fed-bc34-350f220bf202` / `9ef2be2a-d156-4f0c-9572-9c0da5d16b75` | 25/25 | 0 mismatches |
| r17 | portrait / `square_1_1` (final-code rerun) | `c1f77a79-a4c8-4d7d-a94e-1eaaf00ae222` | `ad487607-a308-4e34-a6a4-a5687ead4611` | `a37b6d9b-ad4d-4a99-8c4e-bca696a46dfd` | `6f69073f-c0f1-47da-96a6-1bfd7dcdeb9a` | `50c68d38-dd3d-4684-abac-f8e098991114` / `bb32ac0a-e4db-4106-89d9-0f10eaaab48c` | 25/25 | 0 mismatches |

This is four consecutive PASS runs after the core repair (`r14`–`r17`) on
different media/profile combinations and covers all three allowlisted output
profiles. Portrait was also repeated successfully in r13, r16 and the final
r17 rerun after all source changes. The earlier r8/r9 traces remain available; r10–r12 are retained as
failure evidence rather than being deleted.

## Recovery and failure evidence

- `tools/accept-local.ps1 -Profile local`: PASS. It exercised health/auth,
  direct object upload, Go validation/artifact, SSE replay/snapshot/live/
  reconnect, Python Redis analysis, tracked process restart, and canonical
  Go/Python Job plus SSE recovery.
- Same script with `-Profile lan`, API/web `http://192.168.1.19:8080` and
  `http://192.168.1.19:3000`: PASS for the server path, including restart and
  `lan_server_path=true`, `lan_web_path=true`.
- `tools/accept-lan-client.ps1`: NOT_PASS by design on this host, with a
  machine-identity guard. It must be rerun from a different physical device.
- r10/r11/r12 are real failed Product API traces. r10 exposed portrait dark/
  black scene selection; r11 exposed an over-strict first fix; r12 was a
  transient render failure despite a direct renderer replay passing. r13 and
  r16 demonstrate portrait recovery; no failed evidence was overwritten.
- Provider-negative coverage remains safe: remote API smoke is credential
  blocked and typed failure mapping tests cover missing credential, 401,
  429, 503 and malformed JSON without making an owner-authorized remote call.

## Regression commands

PASS: `tools/verify.ps1 -SkipDesktop`, `tools/verify-gate-h.ps1 -IncludeLive`,
`go test ./...`, `go vet ./...`, `uv lock --project services/ml-worker
--check`, 36 Python tests, Ruff, mypy, Bandit, contract validation, recursive
pnpm typecheck, Rust `cargo fmt --check`/`cargo check`, web production build,
SDK tests, independence/secrets/supply-chain scans, `git diff --check`, and
`python tools/check_memory.py --max-age 0`. The only recurring warning is the
installed Node `24.19.0` being newer than the repository pin `24.14.1`.

## Repairs made in this continuation

1. Timeline selection now excludes low-quality/black rejected scenes without
   excluding valid dark-colored content; a regression test covers the
   portrait failure shape.
2. Product API bootstrap now seeds the same three active system render
   profiles that the Go renderer allowlists (`youtube_16_9`, `shorts_9_16`,
   `square_1_1`).
3. The tracked Python worker startup includes the `analysis` Redis capability;
   this fixed the post-restart acceptance Job that otherwise remained queued.
4. The live acceptance harness records explicit PASS/FAIL status and the
   selected render profile.
5. Provider typing/import annotations were repaired so the canonical static
   checks report their actual green state.

Out of scope remains T605 public/VPS production, DNS/TLS/load, machine
power-loss, remote API quality, WEB_SESSION, and any external-provider billing
or credential setup.

## Prompt continuation evidence — 2026-08-19

The current-tree artifact-level acceptance is recorded separately from the
earlier trace-only runs:

- `real-local-gate-h-live-20260819-r31.json`: landscape, `youtube_16_9`,
  25/25 steps, approved TimelineVersion, completed Render/Render Job and
  complete trace.
- `real-local-artifact-validation-20260819-r31.json`: **PASS**. Every
  referenced artifact downloaded through the signed API URL and matched its
  SHA-256. Script, narration, transcript, subtitle cues/SRT/VTT/ASS, scene
  index/analysis/provenance, embeddings, matching, TimelineVersion, mixed
  audio, render metadata, deliverable QA and 640x360 H.264/AAC output passed.
- `real-local-gate-h-live-20260819-r35.json` and
  `real-local-artifact-validation-20260819-r35.json`: **PASS** for the
  portrait corpus with the same complete artifact checks and a 640x360 output.
- `real-local-render-profile-smoke-20260819-r32.json` and `r33.json`:
  **PASS** for `shorts_9_16` at 360x640 and `square_1_1` at 480x480,
  including worker profile metadata, ffprobe video/audio streams, QA and
  checksums. `real-local-render-idempotency-20260819-r34.json` is **PASS**:
  repeating a completed render returns safe HTTP 412 and creates no Job.

The validator was repaired during this continuation to accept the actual
versioned worker envelope (`state.provenance`) and a single-cue subtitle
payload; diagnostic blackdetect/silencedetect observations are not treated as
acceptance failures. `tools/verify.ps1` now checks native exit codes rather than
printing a broad PASS after a failed `go test` or `pytest`.

The generated edge corpus is recorded in `edge-manifest.json` and
`real-local-edge-scene-detection-20260819.json`: 12 seconds of owned H.264/AAC
media with four 0.5-second cuts, static regions, quiet audio and explicit
silence gaps. Real FFmpeg detection observed silence, and the real scene
detector returned 4 scenes, 4 features and 4 keyframes (**PASS**).

Failure/recovery evidence remains retained: r22/r23 are safe failed jobs at
subtitle generation, r29 is a retained transient portrait render failure, and
the later r31/r35 runs pass after stack restart. `real-local-resource-
observation-20260819.json` records healthy Compose services, API/web listeners,
Ollama/GPU/process observations and no runaway process. The web UI smoke saw
the Product workspace login surface; Tauri `dev` launched `nh-media-desktop`
with no stderr, while cargo/Rust build and typecheck passed. No browser password
was entered, so authenticated UI workflow is not claimed from that smoke.

The physical second-device LAN acceptance is still **NOT_PASS / EXTERNAL INPUT
REQUIRED**: this host is the server and `accept-lan-client.ps1` correctly
rejected same-machine execution. A different physical Windows client is needed
to promote that item to PASS.

## Current-code recovery and final regression — 2026-08-19

- `real-local-gate-h-live-20260819-r38.json` and
  `real-local-artifact-validation-20260819-r38.json`: PASS on the current
  code. The real local Ollama/SAPI/faster-whisper path completed 25/25 steps,
  TimelineVersion approval, signed artifact downloads and SHA-256 validation,
  subtitles/transcript/embeddings/matching/audio/render/QA, with 640x360
  H.264/AAC output.
- `real-local-ai-failure-20260819-r3.json`: intentional Ollama endpoint
  failure. The Product API job reached `failed` in about 13 seconds, retained
  safe `fallback_exhausted` transient error evidence, created no successful
  pipeline output and did not remain stuck in retrying. This exposed and
  repaired duplicate Job transition handling in the retry sweeper.
- `real-local-service-restart-matrix-20260819-r38.json`: PASS for PostgreSQL,
  Redis, MinIO and tracked API/Go/Python/web restart readiness with durable
  completed-job state preserved. `real-local-resource-observation-20260819-r2.json`:
  PASS for healthy Compose, API/web listeners, Ollama/GPU, observed Python
  workers, Redis pending=0 and no runaway process.
- Final `tools/verify.ps1` and `tools/verify-gate-h.ps1 -IncludeLive` pass with
  the reviewed FFmpeg/ffprobe pair; Python has 37 passing tests. The restart
  evidence is between completed jobs, not in-flight/power-loss chaos proof.

The strict Prompt acceptance status remains **NOT_PASS** until a different
physical Windows client completes the current-tree LAN workflow. Authenticated
UI submission was also not performed because no password was entered into the
browser smoke; no claim is made for that path.

## Final current-tree active restart — 2026-08-19

`real-local-active-restart-probe-20260819.json` is **PASS**: PostgreSQL, Redis,
MinIO and the tracked API/Go/Python/web process set were restarted while the
job was active; each returned ready and the job reached `completed`.

`real-local-recovery-fencing-20260819.json` records durable attempt fencing,
Redis pending-result reclaim and SHA-256-verified idempotent final-object
promotion after replay. The final regression and Gate H live wrapper also
passed on the current tree with the reviewed FFmpeg/ffprobe pair.

This is server-side Local/LAN evidence only. Overall Prompt acceptance remains
**NOT_PASS / EXTERNAL INPUT REQUIRED**: a different physical LAN client and an
authenticated browser UI submission were not available in this run; no
power-loss or isolated-worker crash claim is made.

## Authenticated web login smoke — 2026-08-19

`web-login-smoke-20260819.json` is **PASS**. Computer Use logged into the LAN
web client at `http://192.168.1.19:3000` using the configured Local Admin and
reached `Workspace dashboard`; Projects loaded and `Sign out` was visible.
The SDK default `fetch` receiver repair was validated by four SDK tests, web
typecheck and a successful LAN stack restart. Chrome's save-password prompt was
not accepted, and no project/upload/job mutation was made.
