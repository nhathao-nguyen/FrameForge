---
last_verified: 2026-08-18
source: ../SPEC-AUDIT-REPORT.md; ../IMPLEMENTATION-ORDER.md; git metadata
owner: task assignee / release owner
---

# Test evidence

Evidence is append-only where practical and must state limitations.

| Date | Task/check | Command/evidence | Environment/commit | Result | Limitation |
|---|---|---|---|---|---|
| 2026-08-18 | Gate F current working-tree repair audit and final verification | `go test ./... -count=1`; `go vet ./...`; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1`; SDK tests/typecheck, web typecheck/build; `python tools/check_memory.py --max-age 0` | HEAD `85748e69568f14487f81ed1c9890e5a711e6939e`; uncommitted implementation working tree; Windows | pass: exact movie_recap graph/role/schema regression, canonical Timeline schema plus semantic/safety/reference validation, immutable server-authoritative TimelineVersion edit/reload/undo, Go/Python/TypeScript/Rust/security/contract/independence/supply-chain checks and memory validation | desktop signed artifacts were not rebuilt in this run because owner signing password is external input |
| 2026-08-18 | Gate F T550 current local end-to-end acceptance | external env loader invoking `tools/accept-local.ps1 -Profile local`; tracked `tools/dev.ps1 -Action restart` recovery | Windows; PostgreSQL/Redis/MinIO Compose volumes preserved; current working tree; Go API/media worker; real `uv` `nh_media` worker | pass: health/auth/Workspace, FFmpeg+ffprobe fixture, direct MinIO upload/ETag/complete, Go validation Job and Artifact path, SSE snapshot/live/reconnect, Product API-created Python Job through Redis Streams, and both durable Jobs/SSE snapshot recovered after restart | deterministic worker fixture; no external provider/ML quality claim; this run is local-only |
| 2026-08-18 | T434 current-tree signed package freshness check | `tools/accept-t434-install-update.ps1 -Version t434-current -NextVersion t434-current-next -PublicKeyPath C:\Users\PC\.tauri\nh-media.key.pub` | existing per-user staging artifacts; current uncommitted working tree | not pass: verifier rejected `t434-current` because `source/apps/web/src/app/page.tsx` size/hash predates the current working-tree edit | rebuild and re-sign both N/N+1 packages with the owner private-key password before release handoff; this is an external signing-input/artifact freshness issue, not an implementation test failure |
| 2026-08-18 | Gate F current-tree T550 physical second-device acceptance | current `C:\Users\Public\accept-lan-client-current.ps1`; evidence `C:\Users\Public\nh-media-lan-client-current.json` | external Windows client `DESKTOP-92ICS6C` at `192.168.1.29`; server `192.168.1.18`; Windows PowerShell `5.1.19041.2673`; current script hash matched working tree | pass: run `lan-client-1353be68-e9ba-41d7-9ee1-b2f97340452e`, `status=PASS`, `external_to_server=true`, authentication, Workspace/Project, upload initiation/abort, completed Job visibility, REST event replay, SSE snapshot/reconnect, no local backend dependency and cleanup | deterministic worker result; no external provider/ML quality claim |
| 2026-08-18 | Gate F current-tree T550 local acceptance | `tools/dev.ps1 -Profile local -Action restart`; `tools/accept-local.ps1 -Profile local -ApiBaseUrl http://127.0.0.1:8080` | Windows; PostgreSQL/Redis/MinIO Compose; current working tree; Go API/media worker; real `uv` `nh_media` worker | pass: health/auth/Workspace, FFmpeg+ffprobe fixture, direct MinIO PUT/ETag/complete, dedicated asset-validation Job with exact `completed`, Go worker path, SSE snapshot/live/reconnect, Product API-created analysis Job through Redis Streams to Python with exact `completed`, and the same Go/Python Jobs plus SSE snapshot recovered after tracked app restart | deterministic worker fixture; no external provider/ML quality claim; same run does not prove physical second-device LAN or T434 install/update |
| 2026-08-18 | Gate F current-tree T550 LAN server acceptance | `tools/dev.ps1 -Profile lan -LanServerIp 192.168.1.18 -Action restart`; `tools/accept-local.ps1 -Profile lan -ApiBaseUrl http://192.168.1.18:8080 -LanBaseUrl http://192.168.1.18:8080 -LanWebBaseUrl http://192.168.1.18:3000` | Windows LAN server `192.168.1.18`; current working tree | pass: exact LAN API/web health plus the same upload/Go/Python/Redis/SSE path; both durable Jobs and SSE snapshot recovered after tracked app restart; server script explicitly reports physical second device as not run | same-host LAN traffic is server-path evidence only and is not a second-device PASS |
| 2026-08-18 | Gate F current-tree native workflow activation | read-only PostgreSQL query of active workflows/pipelines/nodes after current API bootstrap | live private PostgreSQL; current working tree | pass: `asset_validation` 1 node, `acceptance_analysis` 1 node, canonical `movie_recap` v3 25-node graph active, and prior immutable movie versions deprecated; v3 was required because the durable v2 content hash differed from the current policy-complete graph | full provider/media execution remains outside Gate F; a current two-version signed desktop package is still required for T434 |
| 2026-08-18 | T434 current-tree signed install/update acceptance | signed `t434-current` and `t434-current-next` packages with owner key; `tools/accept-t434-install-update.ps1 -Version t434-current -NextVersion t434-current-next -PublicKeyPath C:\Users\PC\.tauri\nh-media.key.pub` | current working tree; exact LAN origin `http://192.168.1.18:8080`; NSIS; owner key/password entered in process only | pass: same public-key identity, package verification, fresh per-user N install/launch, N→N+1 install/launch, tamper rejection and pointer rollback N+1→N | no physical second-device T550 claim; provider/ML quality remains outside deterministic acceptance |
| 2026-08-16 | Upstream research provenance | recorded git identity/tag/tree/inventory inspection | external checkout at `bc2d276cf477fe3ce1a16f9679dcb1d2978e3a74` | pass at inspection time | not reverified; not runtime/parity/rollback evidence |
| 2026-08-17 | Starting repository boundary | branch/status/HEAD inspection | `docs/architecture-spec` at `1cab87bbfd2801aa552948f8e8f8db9b3e2ffb0c` | clean before refactor | proves starting state only |
| 2026-08-17 | T000 documentation consistency | `python tools/check_memory.py --max-age 0` | current working tree | pass: 12 memory files, 15 OQs, 81 tasks; all internal links, Markdown tables and JSON/JSONL fences valid | documentation structure only; no runtime behavior |
| 2026-08-17 | Capability/terminology audit | repository-wide `rg` classifications plus matrix class counts | current working tree | pass: 25 REIMPLEMENT, 6 ADOPT-CONCEPT, 11 IMPROVE, 1 REFERENCE, 10 DEFER, 3 IGNORE; remaining legacy/version terms are negative, provenance or generic native concepts | semantic review, not a source-license opinion |
| 2026-08-17 | Change hygiene | `git diff --check`; trailing-whitespace and secret-assignment scans; status/file-boundary/reference-tree checks | current working tree on `docs/architecture-spec` | pass: docs plus validator only; no vendored upstream tree or application source | Git reports expected LF-to-CRLF working-copy warnings only; no commit/push/merge performed |
| 2026-08-17 | T004 final specification consistency | `python tools/check_memory.py --max-age 0` plus validator syntax compile | current working tree | pass: 12 memory files, 15 closed OQs, 83 mirrored tasks; links/tables/JSON fences/dependencies valid | documentation and validation tooling only; not runtime acceptance |
| 2026-08-17 | Decision closure and final gate | unresolved-status/false-blocker `rg` scan; final-status assertion scan; OQ mirror count | current working tree | pass: no OPEN/BLOCKED owner/spec status, 15 allowed OQ statuses, required SPEC/NEXT assertions present | historical/negative upstream and canonical JobStep `blocked` terms remain intentionally |
| 2026-08-17 | Final change boundary | `git diff --check`; changed-path allowlist; upstream-runtime name scan outside documentation | current working tree on `docs/architecture-spec` | pass: Markdown plus `tools/check_memory.py` only; no application/runtime/upstream source | expected Git LF-to-CRLF warnings only; no commit/push/merge performed |
| 2026-08-17 | T002 Windows toolchain bootstrap | `git/go/python/uv/node/pnpm/rust/tauri/ffmpeg/ffprobe/docker` version checks; MSVC/WebView2 checks; paired FFmpeg encode/probe smoke | `implementation/bootstrap` at `5df7308`, Windows 11 Pro PowerShell 7.6.4 | pass: selected pins and hashes recorded in `docs/bootstrap/T002-TOOLCHAIN-MATRIX-WINDOWS.md` | no application module, lockfile, ML environment or compose stack created |
| 2026-08-17 | T003 reference-behavior policy | JSON manifest parse/schema/empty-fixture assertion; absent `references` checkout check; policy review | `implementation/bootstrap` at `5df7308` | pass: optional recorded research only; normal upstream execution prohibited | no fixture comparison is claimed; manifest intentionally empty |
| 2026-08-17 | T004 final pre-code certification | memory/link/table/JSON checks; task/OQ/status parity; forbidden upstream/source scan; `git diff --check` | current working tree on `implementation/bootstrap` | pass: T002/T003 complete, T100 eligible, no application code | Docker and Tauri prerequisites are environment evidence, not runtime acceptance |
| 2026-08-17 | T100 repository skeleton | `go test ./...`; `uv run --project services/ml-worker pytest`; `pnpm typecheck`; `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` | uncommitted Gate B working tree on Windows PowerShell | pass: approved service/client/contract boundaries compile/import; no Gate C workflow | Tauri uses a generated deterministic placeholder icon until client branding task |
| 2026-08-17 | T101 shared primitives | Go primitive tests; Python shared fixture test; TypeScript SDK typecheck; `python tools/validate_contracts.py` | Windows PowerShell, Python 3.13.14, Go 1.26.5, Node 24.x | pass: opaque IDs, UTC timestamps, media ranges, revisions/ETags, safe errors and redaction fixtures agree | JSON Schema validation is complemented by language validators; full domain schema corpus is later work |
| 2026-08-17 | T102 config/redaction | API config/SecretStore Go tests; ML config tests; `tools/verify-secrets.ps1` | uncommitted Gate B working tree | pass: namespaced config, encrypted secret record boundary and redacted fields; `.env.example` placeholders only | durable SecretStore repository belongs to Gate C |
| 2026-08-17 | T103–T105 private infrastructure | `docker compose ... up -d`; health inspection; app-role migration denial; migration-role probe; Redis auth; MinIO private bucket/object/range/anonymous policy; service restart | Docker Desktop 4.82.0 / Compose v5.3.0, named volumes `nh_media_*` | pass: PostgreSQL, Redis and MinIO start/health/restart; internal services expose no host ports | volumes are development state and remain until explicit cleanup; no product data was used |
| 2026-08-17 | T106 API shell | `go test ./...`; real `go run .\services\api\cmd\api`; PowerShell HTTP smoke for `/api/v1`, `/live`, `/ready`, LocalAuth login and protected diagnostics | Windows loopback `127.0.0.1:18080` | pass: request/correlation IDs, exact CORS, safe errors, auth and bounded HTTP shell | no PostgreSQL/Redis/product domain routes or long-running media work |
| 2026-08-17 | T107 readiness | `services/api/internal/httpapi` tests | Go httptest with mocked dependencies | pass: dependency-down, timeout and drain return 503 while liveness remains 200 and details stay minimal | dependency adapters are mocked until later persistence/queue tasks |
| 2026-08-17 | T108 CI/independence | `tools/verify.ps1`; Ruff/mypy/Bandit; Go vet/test; pnpm typecheck; Tauri cargo check; lock/SBOM/secret/compose checks; negative fixture expected failure | Windows PowerShell, uncommitted Gate B working tree | pass: baseline green; intentional forbidden-reference fixture fails closed | public CI runner and full image/SBOM publication are later release evidence |
| 2026-08-17 | T200 migration runner | `go test ./services/api/internal/persistence`; cross-compiled `cmd/migrate` against isolated PostgreSQL database; runner repeated; existing dev DB marker reconciliation/repeat; app-role probe | Docker PostgreSQL `nh-media-foundation-postgres-1`, temporary PostgreSQL plus existing `nh_media`, current working tree | pass: seven migrations applied/repeated with seven clean rows; `nh_media_app` SELECT works while `CREATE TABLE` is denied; temporary database removed | no production migration service is claimed |
| 2026-08-17 | T201–T210 domain/persistence | `go test ./...`; SQL migration apply/repeat; durable scoped repositories, hashed session store, append-only event/outbox and idempotency implementations; upstream/secret scans | Windows PowerShell, current working tree | pass: seven migration files and domain/repository packages compile; suspended/inactive scope checks fail closed; no T300 code added | execution transitions/publisher remain Gate E |
| 2026-08-17 | T220–T224 storage/probe | LocalStorage/artifact/probe unit corpus plus opt-in `go test ./services/api/internal/storage -run TestS3StorageConformance -count=1 -v` against private temporary MinIO | Windows PowerShell, Docker private network, current working tree | pass: traversal/symlink-safe roots, ranges, stage/promote/orphan compensation, spoof/size quarantine, live private multipart/presign/range/ACL/expiry/abort conformance | ffprobe execution corpus and worker orchestration remain later execution work |
| 2026-08-17 | T230–T235 native Product API | `go test ./...`; OpenAPI/Timeline parse; durable runtime smoke for Login/Project/Asset/upload/complete; direct MinIO PUT; idempotency replay; API restart session survival | Windows PowerShell, private PostgreSQL + MinIO, current working tree | pass: native `/api/v1` routes use durable backend when configured; Asset remains `validating`, durable `asset_probe` Job is created, same idempotency key replays one Project, OpenAPI has 30 paths | no T350 Job/Run/Step command surface is claimed |
| 2026-08-17 | Gate B+C+D regression | `npm.cmd run verify` plus `git diff --check` and OpenAPI/Timeline parse | Windows PowerShell, branch `implementation/bootstrap`, uncommitted working tree | pass: Go/Python/TypeScript/Rust, independence, secret, lock/SBOM, compose, whitespace and contract parse checks; Gate B baseline remains green | this is local acceptance evidence, not public production certification |

| 2026-08-17 | Durable auth/upload acceptance | cross-compiled API runtime in PostgreSQL container; Login → `/auth/session`, API restart → same bearer session; direct signed MinIO PUT/ETag → complete | `nh-media-foundation-postgres-1` + private `nh_media_private` MinIO, current working tree | pass: hashed `auth_sessions` survives restart; `asset_uploads=completed`, Asset=`validating`, Job=`created/asset_probe`, completed size 12; test rows soft-deleted/cleaned where applicable | no public/LAN hardening or worker execution claimed |
| 2026-08-17 | Gate C+D repair T224 | `go test ./services/media-worker/internal/probe ./services/api/internal/persistence`; deterministic generated fixtures/stubs for spoofed, oversized, malformed container, invalid JSON/stream, stream/output limits and timeout, plus SQL Asset readiness predicate | current working tree after `4e97c9f`; Windows | pass: quarantine/safe reason classification; `Validated` remains false for every rejected/pathological case and true only after complete probe checks; uncommitted Artifact cannot make an Asset `ready` | no large media corpus or worker orchestration was added |
| 2026-08-17 | Gate C+D repair T232/T233 | `go test ./services/api/internal/domain ./services/api/internal/product ./services/api/internal/persistence ./services/api/internal/httpapi`; Store negative corpus plus durable resolver/RenderProfile sqlmock tests | current working tree after `4e97c9f`; Windows | pass: ownership/state resolution is fail-closed; durable create/version/approval/lock/validate/render paths use resolver; missing/ineligible RenderProfile does not auto-create | live migration/infrastructure rerun recorded below |
| 2026-08-17 | Gate C+D repair regression | `go test ./...`; `npm.cmd run verify`; `git diff --check` | current working tree, Windows | pass: Go, full canonical verification including Rust desktop check, independence/secrets/supply-chain/contract checks and whitespace gate | Node engine warning only; no verification failure |
| 2026-08-17 | Gate C+D live infrastructure repair | `docker compose --file infrastructure/compose/docker-compose.yml --profile local up -d --wait`; migration runner apply/repeat in private network; app-role DDL denial; live `TestS3StorageConformance` | Docker Desktop 4.87.0 / Compose v5.4.0, `nh_media_private`, current working tree | pass: PostgreSQL/Redis/MinIO healthy; all 8 migrations including 0008 clean/repeated; app role cannot CREATE TABLE; private MinIO multipart/presign/range/ACL/expiry/abort conformance pass | local named volumes remain as development state |
| 2026-08-17 | T300 transitions | `go test ./services/api/internal/execution ./services/api/internal/persistence -run 'Transition|Event' -count=1` | Windows, HEAD `795c2de` plus uncommitted Gate E | pass: canonical Job/Run/Step transition graphs, invalid/terminal rejection, transactional scoped SQL update and event/outbox append, conflict rollback | SQL tests use sqlmock; live concurrent PostgreSQL race evidence remains later runtime work |
| 2026-08-17 | T301 outbox/replay | `go test ./services/api/internal/execution ./services/api/internal/persistence -run 'Outbox|Replay' -count=1` | Windows, current working tree | pass: bounded publisher, stable message IDs, SKIP LOCKED claim, ordered scoped replay, safe retry error | no long-running live publisher process acceptance claimed |
| 2026-08-17 | T310–T313 queue/scheduler/lease/process | `go test ./services/api/internal/queue ./services/api/internal/scheduler ./services/api/internal/execution ./services/media-worker/internal/process ./services/api/internal/persistence` | Windows, current working tree | pass: Redis Streams adapter compiles, deterministic DAG frontier, hashed lease boundary, expiry reconciliation adapter, argv-only bounded ffmpeg/ffprobe process policy | Queue and lease SQL paths need live Redis/PostgreSQL worker corpus |
| 2026-08-17 | T320 contracts | `python tools/validate_contracts.py`; `go test ./packages/shared-contracts/go/worker`; `uv run --project services/ml-worker pytest`; Ruff/mypy | Windows, current working tree | pass: versioned worker command/result/progress schemas and Go/Python validators agree; fixtures and forbidden path/secret/traceback fields reject | contract validation is not a production worker run |
| 2026-08-17 | T321 probe/thumbnail node | `go test ./services/media-worker/internal/node ./services/media-worker/internal/process ./services/media-worker/internal/probe` | Windows, current working tree | pass: probe/report and thumbnail node paths stage only Artifact refs; quarantine emits no output; process policy remains sandboxed | node test doubles do not prove live MinIO/FFmpeg acceptance |
| 2026-08-17 | T322/T323 cross-language slice | `go test ./services/media-worker/internal/integration -run TestPythonWorkerContract -count=1`; `uv run --project services/ml-worker pytest` | Windows, current working tree | pass: Go launches local `uv` without a shell, Python `nh_media` validates command and returns a safe deterministic result; durable Job bootstrap/control/SSE routes compile and unit-test | Redis queue consumption, Artifact commit and end-to-end API-to-worker runtime are still missing |
| 2026-08-17 | T323 live Redis Streams worker | ephemeral `redis:7.4.5-alpine`; `NH_MEDIA_TEST_REDIS_ADDR=127.0.0.1:6380 go test ./services/api/internal/integration -run TestPythonWorkerRedisRuntime -count=1 -v` | Windows, disposable loopback Redis, current working tree | pass: ID-only execution stream, controller second-hop command, real `uv`/`nh_media` Redis consumer, validated result stream and post-result ACK; worker subprocess tree terminated and Redis container removed | this harness does not combine the Python result with the separate durable MinIO/Artifact adapter or PostgreSQL state transition |
| 2026-08-17 | T322 durable worker/Artifact runtime | `docker run --rm --network nh_media_private ... golang:1.26 go test ./services/api/internal/integration -run TestDurableWorkerArtifactRuntime -count=1 -v` with PostgreSQL/Redis/MinIO variables | Docker `nh_media_private`, existing migrated dev services, current working tree | pass: real Project/Job/PipelineRun/JobStep start, Redis delivery, lease claim, empty deterministic report staging/promote, committed MinIO Artifact producer link, `node.completed` event and step completion; fixture Project/Artifact soft-deleted, append-only audit retained | native executor is a deterministic harness; combined Python result-to-durable-commit, terminal aggregate and restart/retry proof remain |
| 2026-08-17 | T323 combined Python durable runtime | ephemeral Postgres/Redis/MinIO containers with repo migrations; `go test ./services/api/internal/integration -run TestPythonWorkerDurableArtifactRuntime -count=1 -v` plus real `uv` worker | Windows host `uv` + three disposable loopback containers, current working tree | pass: ML workflow Job/Run/Step, ID-only queue, lease-aware claim, real `nh_media` Redis worker, result reconciliation, MinIO Artifact promote/link and completed Step; fixture soft-delete and append-only audit retained | restart/retry/redelivery and client/LAN acceptance remain |
| 2026-08-17 | T323 Python worker restart reclaim | ephemeral `redis:7.4.5-alpine`; `NH_MEDIA_TEST_REDIS_ADDR=127.0.0.1:6380 go test ./services/api/internal/integration -run TestPythonWorkerRedisRestartReclaimsPendingEntry -count=1 -v` | Windows, disposable loopback Redis, current working tree | pass: first Python consumer is terminated during a delayed command; second consumer XAUTOCLAIMs the pending entry and publishes/ACKs one validated result | failure-category retry/backoff and combined durable retry remain |
| 2026-08-17 | Gate E post-runtime regression | `npm.cmd run verify`; live Redis Python harness; live durable worker/Artifact harness | Windows plus Docker private network, current working tree | pass: canonical Go/Python/Rust/TypeScript/contract/independence/secret/supply-chain checks and both live execution slices | T323 combined Python Artifact path, T330–T340 recovery/reconnect and T550 client/LAN acceptance remain unclaimed |
| 2026-08-17 | T330–T332 execution safety foundations | `go test ./services/api/internal/execution ./services/api/internal/persistence -run 'Checkpoint|Dead|Retry|Lease' -count=1` | Windows, current working tree | pass: checkpoint compatibility/reuse, bounded retry taxonomy/backoff and atomic dead-letter SQL adapters are covered by focused tests/compilation | crash/resume, review graph, replay/new Job and full cancellation corpus remain |
| 2026-08-17 | T340 SSE boundary | `go test ./services/api/internal/httpapi ./services/api/internal/product ./services/api/internal/persistence` | Windows, current working tree | pass: authenticated bounded snapshot/replay/poll SSE endpoint and REST event replay are present | live queue progress, reconnect and terminal-event acceptance remain |
| 2026-08-17 | Gate E regression | `go test ./...`; `uv run --project services/ml-worker pytest`; Ruff; mypy; `python tools/validate_contracts.py`; `tools/verify-independence.ps1`; `tools/verify-secrets.ps1`; `python tools/check_memory.py` | Windows, branch `implementation/bootstrap`, current working tree | pass: all current Go/Python/contract/independence/secret/memory gates green | T322/T550 full runtime harness, client acceptance, load, backup/restore and public production remain unclaimed |
| 2026-08-17 | T323 durable retry/redelivery runtime | ephemeral Postgres/Redis/MinIO containers with repo migrations; host `uv`; `go test ./services/api/internal/integration -run TestPythonWorkerDurableArtifactRuntime -count=1 -v` | Windows host plus disposable loopback ports 55432/56379/59000, current working tree | pass: real Python worker emits a bounded transient failure on attempt 1; PostgreSQL records retrying Job/Step, retry sweeper waits for policy backoff, requeues an ID-only attempt-2 message, Python completes, MinIO Artifact commits, and Job/Run/Step reach completed; all three containers and worker tree removed | this is one transient retry category; timeout/resource/provider matrix, client disconnect/LAN and crash/resume remain |
| 2026-08-17 | Redis Queue nonblocking poll correction | focused Go queue/execution/integration tests plus the live T323 retry runtime | Windows, current working tree | pass: QueuePort zero-duration polls are bounded instead of translating to Redis `BLOCK 0` forever; retry controller can observe empty polls, shutdown and redelivery safely | no change to Redis/PostgreSQL source-of-truth boundary |
| 2026-08-17 | Post-retry durable native regression | `docker run --rm --network nh_media_private ... golang:1.26 go test ./services/api/internal/integration -run TestDurableWorkerArtifactRuntime -count=1 -v` | existing private PostgreSQL/Redis/MinIO development services, current working tree | pass: native Go lease/result path still commits the MinIO Artifact, producer link, `node.completed` and terminal aggregate after retry/aggregation changes | development services and named volumes left running/unchanged |
| 2026-08-17 | Post-retry full verification | `npm.cmd run verify`; `python tools/check_memory.py`; `git diff --check` | Windows, branch `implementation/bootstrap`, current working tree | pass: Go/Python/Rust/TypeScript, 8 Python tests, Ruff/mypy, contracts, independence, secrets, supply-chain, compose and memory checks; only expected LF-to-CRLF warnings | Node engine warning only; client/LAN, crash/resume, load, backup/restore and public production remain unclaimed |
| 2026-08-17 | T310 queued-message recovery boundary | focused `services/api/internal/queue` and persistence tests | Windows, current working tree | pass: zero-duration Redis polls are bounded and durable `queued` JobSteps can be re-read with stable job/step/attempt IDs after an enqueue interruption; Redis remains delivery-only | live Redis outage/recovery timing corpus remains later T322/T340 evidence |
| 2026-08-17 | Final Gate E local regression after queue recovery | `npm.cmd run verify`; `python tools/check_memory.py`; `git diff --check` | Windows, branch `implementation/bootstrap`, current working tree | pass: complete local verification remains green after queued-frontier recovery; expected Node engine and LF-to-CRLF warnings only | T322/T323 client/LAN, full crash/resume/review/replay/reconnect, load, backup/restore and public production remain unclaimed |
| 2026-08-17 | T322 final native durable runtime | private `nh_media_private` network; `docker run ... golang:1.26 go test ./services/api/internal/integration -run TestDurableWorkerArtifactRuntime -count=1 -v` with PostgreSQL/Redis/MinIO variables | Docker Compose PostgreSQL/Redis/MinIO, current working tree after command/recovery fixes | pass: live Job/Run/Step bootstrap, ID-only Redis delivery, lease claim, MinIO Artifact promote/link, result reconciliation, node completion and post-patch aggregate path | deterministic native result; no media corpus or desktop client claim |
| 2026-08-17 | T323 final Python durable runtime | disposable PostgreSQL/Redis/MinIO containers with migrations; host `uv`; `go test ./services/api/internal/integration -run TestPythonWorkerDurableArtifactRuntime -count=1 -v` | Windows host + ephemeral loopback services, current working tree after `stop_after` aggregate fix | pass: real `nh_media` worker, first transient failure, bounded retry/backoff, attempt-2 redelivery, PostgreSQL Step/Job completion and MinIO Artifact commit | one deterministic transient category; provider/media taxonomy remains later node work |
| 2026-08-17 | T323 worker restart/reclaim | disposable authenticated Redis; `go test ./services/api/internal/integration -run 'TestPythonWorker(RedisRuntime\|RedisRestartReclaimsPendingEntry)$' -count=1 -v` | Windows host + real `uv` worker + Redis Streams | pass: round-trip and XAUTOCLAIM after terminating the first worker; one result and ACK after restart | no production load/chaos claim |
| 2026-08-17 | T330 recovery and T331/T332 command graph | disposable migrated PostgreSQL; `go test ./services/api/internal/integration -run TestDurableExecutionCommandSurface -count=1 -v` | Windows host, PostgreSQL app role, current working tree | pass: durable Review required/approve/reject-edit/resume, atomic cancel Job/Run/Step, immutable replay/new Job with `supersedes_job_id`, plus SQL command/list/run/step projections; recovery planner and checkpoint tests also pass | full power-loss/backup-restore chaos is not claimed |
| 2026-08-17 | T340 SSE boundary | `go test ./services/api/internal/httpapi -run TestJobSSESnapshotReplayReconnectAndInvalidCursor -count=1 -v` | Windows, authenticated in-process HTTP handler, current working tree | pass: snapshot includes Job/Steps, ordered replay reaches terminal `job.cancelled`, reconnect with `Last-Event-ID` replays, malformed cursor returns 400 | loopback/LAN external client and production load remain T550/T603 work |
| 2026-08-17 | T350 command surface | `go test ./...`; durable command harness; HTTP package SSE/route tests | Windows, current working tree | pass: native REST and Store/SQL surfaces cover Job/Run/Step/Review and Render create/list/get/control/retry boundaries; all Go packages green | no T400/Gate F, desktop T550 or public/VPS release claim |
| 2026-08-17 | Gate E final regression | `npm.cmd run verify`; `go test ./...`; `python tools/validate_contracts.py`; `python tools/check_memory.py --max-age 0`; `git diff --check` | Windows, branch `implementation/bootstrap`, pre-checkpoint working tree | pass: Go/Python/Rust/TypeScript/contracts/independence/secrets/supply-chain/memory/whitespace checks green; Node engine warning only | Local/LAN execution slice only; no load, backup/restore, public production or Gate F |
| 2026-08-17 | Gate E checkpoint handoff | `git status --short --branch`; `git rev-parse HEAD`; `git ls-remote origin refs/heads/implementation/bootstrap`; `git diff --check`; `tools/verify-secrets.ps1` | Windows, branch `implementation/bootstrap`, checkpoint `2fa58d4` | pass: worktree clean, local HEAD matches `origin/implementation/bootstrap`, whitespace check and secret scan pass | Gate E Local/LAN slice is checkpointed; T400/Gate F, desktop T550, hardened T603 and public/VPS T605 remain outside this task |
| 2026-08-17 | Final Gate E acceptance repair: T322/T330/T331/T332/T340/T350 | `docker run --rm --network nh_media_private ... golang:1.26 go test ./services/api/internal/integration -count=1 -v` with PostgreSQL/Redis/MinIO variables and real `uv`; focused tests `TestProductAPIDurableClientDisconnectReconnect`, `TestProductAPIDurableStopAfterResumeAndPausedReconnect`, `TestDurableCrashResumeBoundariesAndStaleLease`, `TestDurableCancellationRetryExhaustionDLQAndReplay`, `TestDurableExecutionCommandSurface`, `TestPythonWorkerDurableArtifactRuntime`, `TestPythonWorkerRedisRestartReclaimsPendingEntry`, `TestGateESafeRetryTaxonomyMatrix` | Windows/Docker `nh_media_private`, real PostgreSQL 16, Redis 7, MinIO, Go 1.26, CPython 3.13/uv `nh_media`; baseline `f640524e1e91ed21c3bed66702d3b6b50e55b9fb` | pass: Product API Job creation/start, client loss/reconnect, PostgreSQL authority, Redis delivery/reclaim, MinIO commit, lease expiry/stale rejection, first incomplete resume, pause/review, cancellation, retry/backoff/exhaustion/DLQ/replay, durable SSE snapshot/replay/auth/scope/REST equivalence and Python retry/worker restart all pass | no machine power-loss, public production, external load or desktop-shell claim; Linux acceptance container was explicitly removed after test completion |
| 2026-08-17 | Final canonical verification | `go test ./...`; `uv run --project services/ml-worker pytest`; `python tools/validate_contracts.py`; `python tools/check_memory.py --max-age 0`; `npm.cmd run verify`; `& .\\tools\\verify-independence.ps1`; `& .\\tools\\verify-secrets.ps1`; `git diff --check` | Windows `implementation/bootstrap`, current acceptance working tree | pass: Go, 8 Python tests, contracts, memory, TypeScript, Rust/Tauri, independence, secret, supply-chain and whitespace checks; expected Node engine/LF-CRLF warnings only | no Gate F/T550/T603/T605 work claimed |
| 2026-08-17 | Gate E acceptance checkpoint | `git status --short --branch`; `git rev-parse HEAD`; `git ls-remote origin refs/heads/implementation/bootstrap`; `git diff --check`; `tools/verify-secrets.ps1` | Windows, branch `implementation/bootstrap`, checkpoint `564ef99` | pass: clean checkpoint created after canonical/live PASS; push hash is recorded after remote verification | T400/Gate F, desktop T550, hardened T603 and public/VPS T605 remain outside this task |

| 2026-08-18 | T400–T420 native pipeline/timeline | `go test ./services/api/internal/pipeline ./services/api/internal/domain ./services/api/internal/product ./services/api/internal/persistence`; `gofmt -l packages services` | Windows, uncommitted Gate F working tree | pass: immutable 25-node MovieRecap catalog, validator negative corpus, runtime retry/review/soft-failure/cancel/idempotency, durable typed review resource checks, canonical BuildTimeline proposals/overrides/hash | provider execution and real media/ML nodes remain outside this slice |
| 2026-08-18 | T430–T433 client slice | `pnpm typecheck`; `cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --check`; `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml`; live `cargo-tauri dev` | Windows, Next.js 15.5.3, Tauri 2.11.4, current working tree | pass: SDK REST/SSE reconnect client, ScriptVersion and Timeline command editor forms, thin Tauri shell with loopback CSP and no filesystem/shell/database permissions; `nh-media-desktop.exe` launched | no production bundle/signing claim; LAN CSP requires explicit deployment allowlist |
| 2026-08-18 | Canonical Gate F regression | `powershell -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1 -SkipDesktop`; `go test ./...`; `pnpm typecheck`; `python tools/validate_contracts.py`; independence/secret/supply-chain scans; `git diff --check` | Windows, Docker Desktop 29.6.1 server, uncommitted working tree | pass: Go/Python/TypeScript, Ruff/mypy/Bandit/pytest (8), contracts, independence, secret, lock/SBOM and whitespace checks; expected Node engine/LF-CRLF warnings only | full `verify.ps1` desktop rerun is represented by separate cargo fmt/check results |
| 2026-08-18 | T550 local functional acceptance | `tools/dev.ps1 -Action restart`; `tools/accept-local.ps1 -Profile local` | Windows loopback; PostgreSQL/Redis/MinIO Compose; Go API + Go media worker + real `uv` Python worker + Next web | pass: LocalAuth/default Workspace/project, real MinIO signed PUT/ETag/complete, validating Asset + validation Job start, Go worker result/Artifact path, Python protocol result, SSE replay source, FFmpeg/ffprobe | deterministic worker result; no external provider/ML result claim |
| 2026-08-18 | T550 LAN server-path acceptance | `tools/dev.ps1 -Profile lan -Action restart`; `tools/accept-local.ps1 -Profile lan -LanBaseUrl http://127.0.0.1:8080` | Windows LAN profile; API `0.0.0.0:8080`, web `0.0.0.0:3000` | pass: same authenticated upload/Job/worker/SSE path and explicit LAN health URL; `physical_second_device=external_uncontrolled` | no physical second device was available to control, so this is not a second-device PASS |
| 2026-08-18 | T434 packaging boundary | `tools/package-desktop.ps1 -Action validate`; `-Action rollback`; `-Action package` | Windows, external staging under `%LOCALAPPDATA%\NH-Media\staging` | partial: source boundary manifest and rollback pointer pass; package action fails closed because no release bundle and no external `TAURI_SIGNING_PRIVATE_KEY` | production signing/release artifact remains missing; no signing identity was fabricated |

| 2026-08-18 | T434 unsigned/development package pipeline | `cargo tauri build --config apps/desktop/src-tauri/tauri.conf.json --no-sign --ci`; `tools/package-desktop.ps1 -Action validate`; `tools/package-desktop.ps1 -Action package`; `tools/package-desktop.ps1 -Action verify` | Windows, current working tree, Tauri 2.11.4, NSIS | pass: static Next export copied into Tauri `frontendDist`, one unsigned Windows NSIS bundle built, source/capability/provider-secret/server-compute boundary manifest generated; package and verify fail closed with an exact external-key requirement/unsigned-manifest rejection | this is development/unsigned evidence only; no signed bundle or T434 PASS is claimed |
| 2026-08-18 | T550 live local acceptance after boundary repair | `tools/dev.ps1 -Profile local -Action restart`; `tools/accept-local.ps1 -Profile local` | Windows loopback; PostgreSQL/Redis/MinIO Compose; Go API/media worker; real `uv` Python worker; Next web | pass: health/readiness, LocalAuth/default Workspace, Project, signed MinIO PUT/ETag/complete, validation Job, Go Artifact path, Python protocol result and event replay source | deterministic worker result; no external provider/ML result claim |
| 2026-08-18 | T550 live LAN server path after boundary repair | `tools/dev.ps1 -Profile lan -LanServerIp 192.168.1.18 -Action restart`; `tools/accept-local.ps1 -Profile lan -ApiBaseUrl http://192.168.1.18:8080 -LanBaseUrl http://192.168.1.18:8080 -LanWebBaseUrl http://192.168.1.18:3000` | Windows LAN profile; API `0.0.0.0:8080`; web `0.0.0.0`; server IP `192.168.1.18` | pass: the authenticated upload/Job/worker/SSE path and web/API health were reached through the server LAN address; server script reports physical second device as not run | same-machine request is server-path evidence only, never a second-device PASS |
| 2026-08-18 | T550 physical-client workflow guard | `tools/accept-lan-client.ps1 -ServerLanIp 127.0.0.1`; same script with `-ServerLanIp 192.168.1.18` on the server machine | Windows server machine; evidence JSON written under `%LOCALAPPDATA%\Temp` | pass: loopback and same-host invocations fail closed with `NOT_PASS`; the script records client/server identity, prompts for credentials without writing them and has no local backend dependency | no second physical device was available; external evidence remains pending |
| 2026-08-18 | T550 LAN web restart repair | `tools/dev.ps1` process-tree repair; clean Next dev start on `0.0.0.0:3000`; `Invoke-WebRequest` health probes | Windows LAN server `192.168.1.18`, Next.js 15.5.3 | pass: stale Next.js children were stopped, generated `.next` state was cleared, and web/API health returned HTTP 200 on loopback and LAN addresses | physical client acceptance must still rerun and produce `status=PASS` with `external_to_server=true` |
| 2026-08-18 | T550 physical-client authentication and SSE diagnosis | `tools/accept-lan-client.ps1` on external client `192.168.1.29` | LAN server `192.168.1.18`; external Windows client; evidence runs `lan-client-03f6f5bc-df7e-42ab-98da-41f0101798ab` and `lan-client-a8c2157d-f371-4eb4-bd47-138879933b93` | pass through authentication, Workspace, Project, upload initiation/abort, Job creation/completion and REST event replay; fixed final Windows PowerShell 5.1 SSE compatibility by loading the `System.Net.Http` assembly and using fully qualified types | these runs are not a Gate F PASS until the corrected script is rerun and records SSE snapshot/reconnect PASS |
| 2026-08-18 | Windows LAN portability audit | PowerShell 7 and Windows PowerShell 5.1 parser/runtime checks; RFC1918 negative probes; `tools/package-desktop.ps1 -Action validate -LanServerIp 192.168.1.18`; Tauri exact-CSP release build; binary origin scan; isolated desktop frontend build while LAN web remained live; `npm.cmd run verify` | Windows 11 host; PowerShell 7.6.4 and Windows PowerShell 5.1.26100; Tauri 2.11.4 | pass: all PowerShell tools parse in both runtimes; `System.Net.Http` handler works in 5.1; loopback/public targets fail closed; LAN manifest and compiled desktop binary contain exact `http://192.168.1.18:8080`; copied `.web-build` workspace completed and cleaned while loopback/LAN web stayed HTTP 200; full Go/Python/TypeScript/Rust/independence/security/supply-chain verification passes | final external SSE run remains required; no claim for Windows older than the documented baseline |
| 2026-08-18 | T434 signed staging package | `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/run-t434-signing.ps1` | Windows, external owner key pair under the owner-controlled `.tauri` directory, Tauri 2.11.4, NSIS | pass: static export and release build completed; `NH-Media_0.1.0_x64-setup.exe` size `2053874`, signature sidecar size `420`; manifest signing state and public-key fingerprint recorded; external public-key Minisign verification passed | private key/password are not repository artifacts and are not recorded |
| 2026-08-18 | T434 tamper and rollback acceptance | same signed staging runner; `tools/verify-tauri-signature.mjs` | external staging `C:\Users\PC\AppData\Local\NH-Media\staging\t434-signed` | pass: tampered temporary payload was rejected, signed manifest checksums rechecked, and `current.json` points to signed version `t434-signed` | no installer/update deployment to a user machine is claimed |
| 2026-08-18 | LAN server lifecycle and desktop-origin acceptance | `tools/run-lan-server.ps1 -ServerLanIp 192.168.1.18`; HTTP/CORS probes before and after isolated desktop build | Windows LAN server; API `0.0.0.0:8080`; web `0.0.0.0:3000` | pass: tracked listener PIDs stop/restart deterministically; API live/ready and web loopback/LAN return HTTP 200; exact origins `http://192.168.1.18:3000` and `http://tauri.localhost` are allowed; web remains HTTP 200 after desktop build | fixed ports must be free or owned by the tracked NH-Media state; no wildcard CORS |
| 2026-08-18 | T434 exact-origin LAN signed staging | `tools/run-t434-signing.ps1 -Version t434-lan-signed -LanServerIp 192.168.1.18` | external staging `%LOCALAPPDATA%\NH-Media\staging\t434-lan-signed`; external owner key pair; Tauri 2.11.4; NSIS | pass: manifest profile `lan`, API origin `http://192.168.1.18:8080`, compiled-binary origin scan, external signature verification, tamper rejection and rollback pointer; installer size `2053427`, SHA-256 `222ce82f8be320b1a77b7c14c4bdcec1be86ec8e12d6c2882489067ba1849e18`; sidecar size `420`, SHA-256 `fcb4df5756ef2971098a6bb2ecb81a82c9910c55aaf6d0eca066aa2df80cd97c` | signing key/password remain external and are not recorded; physical-client PASS remains separate T550 evidence |
| 2026-08-18 | T550 physical second-device LAN acceptance | `powershell.exe -NoExit -NoProfile -ExecutionPolicy Bypass -File C:\Users\Public\accept-lan-client.ps1 -ServerLanIp 192.168.1.18 -EvidencePath C:\Users\Public\nh-media-lan-client.json` | server `192.168.1.18`; external client `DESKTOP-92ICS6C` at `192.168.1.29`; Windows `10.0.19045.0`; Windows PowerShell `5.1.19041.2673`; run `lan-client-a384adea-f665-4cd0-ba01-1035e8acfef2` | pass: runtime compatibility, API/web TCP and HTTP health, interactive authentication, Workspace/Project, upload initiation/abort, completed Job visibility, REST event replay, SSE initial snapshot/reconnect replay, no local backend dependency and project cleanup; `status=PASS`, `external_to_server=true` | sanitized evidence: `docs/evidence/t550-lan-client-pass-20260818.json`, repository SHA-256 `7570210c2c9041ece363330192381a0999f63c1e4d5b762facdaa0b1e307f6f2`; source transport SHA-256 `a472b618ded67cbae9d7aa343ef2e1741f7a58f274272322f7280cc74bf02f10`; no credential/token present |
| 2026-08-18 | Gate F final acceptance and regression | `tools/accept-local.ps1 -Profile local -ApiBaseUrl http://127.0.0.1:8080`; same script with `-Profile lan` and exact `192.168.1.18` API/web URLs; `tools/package-desktop.ps1 -Action verify -Version t434-lan-signed -LanServerIp 192.168.1.18`; `npm.cmd run verify`; `go test ./... -count=1`; focused pipeline/integration tests; recursive pnpm typecheck; contracts/memory checks; cargo fmt/check | current uncommitted Gate F working tree; live LAN stack; signed external staging package | pass: local and LAN acceptance include health/auth/Workspace, real FFmpeg fixture, direct object upload, Go worker/Artifact, Python worker and SSE source; signed package verifies; canonical and explicit Go/TS/Python/Rust/security/independence/supply-chain checks pass | expected Node engine warning (`24.14.1` requested, `24.19.0` current) and LF/CRLF warnings only; no Prompt 6/Gate G work claimed |

## Gate F status

T400–T434, T550 local/LAN-server acceptance and the physical Windows second-device run are reviewable
in the current working tree. Current exact-origin LAN signing, manifest/signature verification,
tamper rejection, rollback, external-client auth/Job/event/SSE reconnect and cleanup pass.

**LOCAL FUNCTIONAL ACCEPTANCE: PASS**

**GATE F: PASS**

T400–T434 and T550 local/LAN/physical-client acceptance pass, including current-tree signed
install/update/tamper/rollback and the external client’s authentication, Job/event/SSE reconnect and
cleanup checks. No public production, load, backup/restore, or external provider/ML quality evidence
is claimed.

| 2026-08-18 | Gate G dependency preflight | actual Windows host inspection; pinned `go`, `rustc/cargo`, Python/uv, Node/pnpm, Tauri CLI, FFmpeg/ffprobe and Docker version commands; `nvidia-smi`; Docker local Compose health | current Windows machine; no previous-machine installation assumption | pass: missing pinned uv 0.11.28, Node 24.14.1, Tauri CLI 2.11.4 and reviewed FFmpeg/ffprobe were installed/verified; Docker daemon and PostgreSQL/Redis/MinIO local profile became healthy; system Python 3.14 and unapproved external providers were not used | no approved provider credentials/model runtime; deterministic fake/local providers are the Gate G boundary |
| 2026-08-18 | Gate G capability reconciliation | `docs/evidence/gate-g-capability-coverage.md`; `docs/IMPLEMENTATION-ORDER.md`; `docs/OPEN-QUESTIONS.md`; `docs/UPSTREAM-CAPABILITY-MATRIX.md` | current repository docs and implementation order | pass: all relevant P5/P6 rows are assigned to T500–T532 or explicit `Txxx-S1` subtasks; karaoke/OCR/object tracking/broad understanding/semantic search/inpainting/batch/scheduling/distributed rendering remain DEFER | no task renumbering or Movie Narrator dependency introduced |
| 2026-08-18 | Gate G Python native workflow | exact pinned uv `lock --check`, frozen sync, `pytest`, Ruff, mypy and Bandit through `tools/accept-gate-g.ps1` | `services/ml-worker`; typed fake providers and in-memory ArtifactStore | pass: 16 tests; typed five-capability conformance, provider-produced TTS/ASR/embedding consumption, real FFmpeg scene detection/features, research/script/style, subtitles/translation/bilingual QA, VLM/characters, matching/coverage, candidates, reference style and audio policy all execute | no external provider/model quality claim |
| 2026-08-18 | Gate G native render/profile/clip proof | exact reviewed FFmpeg/ffprobe paths; `go vet ./...`; `go test ./...`; focused `TestRenderRealMediaAndReuseThreeProfiles`, `TestRenderRealMultiClipMovieWithAudioAndSubtitles`, `TestInspectRejectsMissingVideo` and Artifact commit-after-QA test | Go `services/media-worker/internal/render`; synthetic FFmpeg sources and subtitle/font scratch inside executor sandbox | pass: real 16:9, 9:16 and 1:1 outputs reuse one source fingerprint; typed `ExportClips` produces a real 9:16 selected clip with exact range/profile/codec/checksum manifest; three-clip audio/subtitle render passes duration/dimensions/codecs/audio QA; missing deliverable and pre-QA commit fail closed | exact reviewed FFmpeg build; external provider/media quality is not claimed |
| 2026-08-18 | Gate G implementation repair audit | `tools/accept-gate-g.ps1 -FfmpegPath <reviewed> -FfprobePath <reviewed>`; focused Go render suite includes `TestRenderRealMultiClipMovieWithAudioAndSubtitles`; Python suite includes `test_real_ffmpeg_scene_detection_and_features_are_not_fixture_constants` | requested baseline `7f889b65840631f7605c2d73c94b46a35c1a0a9d`; implementation checkpoint `a645ef3`; reviewed FFmpeg/ffprobe hashes enforced by runner | pass: 16 Python tests, Ruff, mypy, Bandit; Go vet/full tests plus real 3-clip audio/subtitle render, 16:9/9:16/1:1 reuse, clip export and ffprobe QA; Node/pnpm typecheck; cross-language contracts; independence/secrets/supply-chain; Compose config; diff check | deterministic/local provider conformance only; no external provider credentials/model quality, T600–T605 or public production claim |

## Gate G status

**GATE G: PASS**

T500–T532 and the reconciled P5/P6 subtasks are locally executable and reviewable. This is a
provider-free deterministic acceptance plus real reviewed-FFmpeg media/clip QA; it is not a claim of
external provider quality, approved ML-model execution, public production, or the intentionally
deferred matrix capabilities.
